import { h as o, version as l } from "vue";
const m = (a) => ({
  props: {
    className: String,
    color: String,
    size: { type: [String, Number], default: 24 }
  },
  data() {
    return {
      svgData: a
    };
  },
  render() {
    const { className: r, color: e, size: i, svgData: h } = this, c = {
      stroke: "currentColor",
      fill: "currentColor",
      "stroke-width": "0",
      xmlns: "http://www.w3.org/2000/svg"
    }, s = (t) => o(
      t.tag,
      l.startsWith("2.7.") ? {
        attrs: {
          ...c,
          ...t.attr,
          height: i,
          width: i,
          className: r,
          color: e
        }
      } : {
        ...c,
        ...t.attr,
        height: i,
        width: i,
        className: r,
        color: e
      },
      t.child.map((n) => n.child && n.child.length > 0 ? s(n) : o(
        n.tag,
        l.startsWith("2.7.") ? { attrs: { ...n.attr } } : { ...n.attr }
      ))
    );
    return s(h);
  }
});
function w(a) {
  return (r) => o(u, { svgData: a, ...r });
}
const u = {
  props: {
    className: String,
    color: String,
    size: { type: [String, Number], default: 24 },
    svgData: { type: Object }
  },
  render() {
    const { className: a, color: r, size: e, svgData: i } = this, h = {
      stroke: "currentColor",
      fill: "currentColor",
      "stroke-width": "0",
      xmlns: "http://www.w3.org/2000/svg"
    }, c = (s) => o(
      s.tag,
      l.startsWith("2.7.") ? {
        attrs: {
          ...h,
          ...s.attr,
          height: e,
          width: e,
          className: a,
          color: r
        }
      } : {
        ...h,
        ...s.attr,
        height: e,
        width: e,
        className: a,
        color: r
      },
      s.child.map((t) => t.child && t.child.length > 0 ? c(t) : o(
        t.tag,
        l.startsWith("2.7.") ? { attrs: { ...t.attr } } : { ...t.attr }
      ))
    );
    return c(i);
  }
};
export {
  w as GenIcon,
  u as IconBase,
  m as useGenIcon
};
